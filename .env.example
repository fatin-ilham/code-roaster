# Copy this to .env and customize

# AI Model to use (must be installed in Ollama)
MODEL=qwen2.5:3b

# Flask settings
FLASK_ENV=production
FLASK_PORT=5000
FLASK_HOST=0.0.0.0

# Rate limiting (requests per minute per IP)
RATE_LIMIT=10

# Cache settings
CACHE_ENABLED=true
CACHE_DIR=.cache
